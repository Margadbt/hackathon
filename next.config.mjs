/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        formats: ['image/webp'],
        disableStaticImages: true
    }
};

export default nextConfig;
